### Problem No 34: Find maximum between two numbers using switch.

**Problem:**
Find maximum between two numbers using switch.

**One-line Answer:**
Use ternary logic inside `switch(true)` (using workaround).
